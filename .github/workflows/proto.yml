name: Protocol Buffers CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'proto/**'
      - 'scripts/generate-proto.sh'
      - '.github/workflows/proto.yml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'proto/**'
      - 'scripts/generate-proto.sh'
      - '.github/workflows/proto.yml'

jobs:
  validate:
    name: Validate Protocol Buffers
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Buf
        uses: bufbuild/buf-setup-action@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Create buf.yaml if not exists
        run: |
          if [ ! -f proto/buf.yaml ]; then
            cat > proto/buf.yaml << 'EOF'
          version: v1
          breaking:
            use:
              - FILE
          lint:
            use:
              - DEFAULT
          EOF
          fi

      - name: Buf lint
        working-directory: proto
        run: buf lint
        continue-on-error: true

      - name: Buf breaking change detection
        if: github.event_name == 'pull_request'
        working-directory: proto
        run: buf breaking --against "https://github.com/${{ github.repository }}.git#branch=main"
        continue-on-error: true

  generate:
    name: Test Proto Generation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Install protoc
        uses: arduino/setup-protoc@v3
        with:
          version: "29.4"
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Go plugins
        # Pin to the same versions used by docker/api-gateway/Dockerfile and
        # scripts/build-with-docker.sh so the regenerated files match what is
        # committed (otherwise tool drift produces spurious diffs).
        run: |
          go install google.golang.org/protobuf/cmd/protoc-gen-go@v1.36.6
          go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@v1.5.1

      - name: Generate Go code
        run: |
          chmod +x scripts/generate-proto.sh
          ./scripts/generate-proto.sh --go-only

      - name: Check for uncommitted changes
        run: |
          git diff --exit-code || {
            echo "Generated proto files have uncommitted changes"
            echo "Please run './scripts/generate-proto.sh --go-only' and commit the changes"
            exit 1
          }
